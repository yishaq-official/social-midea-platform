{
  "name": "social-midea-root",
  "version": "1.0.0",
  "scripts": {
    "dev": "npm run dev --prefix client & npm run dev --prefix server",
    "install-all": "npm install --prefix client && npm install --prefix server",
    "build": "npm run build --prefix client"
  }
}
